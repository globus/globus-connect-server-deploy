# This YUM configuration file is used in the base image to install packages from
# the target distrubtion. This directory is copied into the base image at
# YUM_CONF_DIR.

[main]
reposdir=%YUM_CONF_DIR%/yum.repos.d
